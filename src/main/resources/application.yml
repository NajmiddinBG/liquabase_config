spring:
  liquibase:
    enabled: true
  datasource:
    url: jdbc:h2:file:~/liquibase
    username: friday
    password: 1113
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: none
    show-sql: true
  h2:
    console:
      enabled: true
server:
  port: 8000
